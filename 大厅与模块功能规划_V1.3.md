# vCampus 工作台与模块功能规划 V1.3

本文档用于说明 vCampus 客户端从登录、进入工作台、访问各业务模块到管理员功能的整体设计。当前本地工程已按本文档完成第一版工作台、强制修改初始密码和模块管理员授权基础能力。

## 一、总体设想

vCampus 客户端采用一个统一入口：

```text
登录页 -> 主界面工作台 -> 点击左侧导航 -> 进入具体业务模块
```

所有业务模块不再各自创建独立窗口，而是统一接入主界面：

```text
LoginFrame
MainFrame
DashboardPanel
各模块 JPanel
```

登录成功后，系统根据当前用户身份和权限决定左侧导航里能看到哪些模块入口。

## 二、登录与账号规则

### 1. 登录页

登录页需要实现：

- 输入登录名
- 输入密码
- 点击登录
- 连接服务端
- 调用 `USER_LOGIN`
- 登录成功后进入工作台
- 登录失败时给出错误提示

登录页不要直接操作数据库，只能通过 `UserClientService` 调用服务端。

### 2. 注册页

现实中的校园系统通常不建议开放普通用户自由注册。

推荐规则：

- 学生账号由系统管理员或学籍管理员创建
- 教师账号由系统管理员创建
- 普通用户不开放随意注册入口
- 如果界面上保留“注册”，第一版建议只作为“申请账号”入口，不直接写入正式账号

原因是学生和教师身份需要和学号、工号、学籍、课程等数据绑定，不能让用户自己随便注册成学生或老师。

### 3. 首次修改密码

如果账号是管理员创建或重置的临时密码，用户登录后必须先修改密码。

流程：

```text
临时密码登录 -> 强制修改密码 -> 修改成功 -> 进入工作台
```

修改密码必须输入：

- 原密码
- 新密码
- 确认新密码

管理员不能直接知道或修改别人的正式密码，只能重置为临时密码。

## 三、工作台首页

工作台是登录成功后的第一屏，负责展示当前用户状态和可访问模块概况。

主界面采用“顶部用户栏 + 左侧导航栏 + 右侧内容区”的结构。模块入口统一放在左侧导航栏，右侧默认显示工作台。

### 工作台基础功能

工作台需要实现：

- 显示当前登录用户姓名
- 显示当前身份，例如学生、教师、系统管理员、模块管理员
- 左侧显示模块导航入口
- 点击左侧导航进入对应模块页面
- 支持退出登录
- 支持返回工作台

### 左侧导航模块入口

当前项目建议包含以下入口：

```text
学籍管理
选课系统
图书馆
校园商店
虚拟银行
宿舍管理
在线课堂
校园BBS
用户管理
```

不同用户看到的入口不完全相同。

## 四、角色与权限设计

当前基础角色仍保留三类：

```text
ADMIN
TEACHER
STUDENT
```

但是“模块管理员”不要直接新增成很多角色，例如 `BOOK_ADMIN`、`SHOP_ADMIN`、`DORM_ADMIN`。更推荐使用“角色 + 模块权限”的方式。

### 推荐模型

`sys_user.role` 表示用户基础身份：

```text
ADMIN      系统管理员
TEACHER    教师
STUDENT    学生
```

另建一张模块管理员授权表：

```text
sys_module_admin
```

建议字段：

```text
module_admin_id
user_id
module_code
status
created_at
```

示例：

```text
user_id=8, module_code=BOOK, status=ACTIVE
user_id=9, module_code=SHOP, status=ACTIVE
user_id=10, module_code=DORM, status=ACTIVE
```

这样一个老师或学生干部也可以被授权管理某个模块，而不必变成系统管理员。

## 五、系统管理员的功能边界

系统管理员的主模块只有：

```text
用户管理
```

系统管理员负责：

- 创建用户账号
- 冻结/启用用户账号
- 重置用户临时密码
- 分配基础角色
- 授权某个用户成为模块管理员
- 查看用户列表
- 查看基础审计记录

系统管理员不直接负责每个业务模块的日常管理。

例如：

- 不由系统管理员直接管理图书库存
- 不由系统管理员直接处理宿舍维修
- 不由系统管理员直接管理商店商品
- 不由系统管理员直接批改在线课堂作业

这样更符合现实中的权限分工。

## 六、模块管理员的功能边界

每个模块可以有自己的管理员。模块管理员只管理自己被授权的模块。

### 1. 学籍管理员

负责：

- 查看学生列表
- 新增学生档案
- 修改学生基本信息
- 修改学籍状态

依赖：

```text
student
sys_user
```

### 2. 选课管理员

负责：

- 创建课程
- 创建开课班
- 修改开课班容量、时间、地点
- 设置选课开放期
- 删除没有关联数据的开课班
- 查看选课情况

普通学生负责：

- 浏览可选课程
- 选课
- 退课
- 查看课表

教师负责：

- 查看自己的开课班名单
- 查看自己的课表

### 3. 图书馆管理员

负责：

- 新增图书
- 修改图书信息
- 下架图书
- 查看借阅记录
- 处理异常借阅状态

普通用户负责：

- 检索图书
- 借阅图书
- 归还图书
- 续借图书

### 4. 商店管理员

负责：

- 新增商品
- 修改商品价格
- 修改商品库存
- 上架/下架商品
- 查看订单

普通用户负责：

- 浏览商品
- 创建订单
- 支付订单
- 取消未支付订单
- 查看自己的订单

商店支付统一使用：

```text
ORDER_PAY
```

商店服务负责订单、库存、银行扣款的统一事务。

### 5. 银行管理员

负责：

- 查看账户列表
- 冻结/解冻异常账户
- 查看交易流水
- 处理异常支付记录

普通用户负责：

- 查看自己的余额
- 充值
- 转账
- 查看自己的流水

银行模块提供扣款能力：

```text
BANK_PAY
BankServerService.payWithinTransaction(...)
```

但商店订单支付由商店模块发起外层事务。

### 6. 宿舍管理员

负责：

- 维护楼栋
- 维护房间
- 分配床位
- 调整床位
- 处理报修单

学生负责：

- 查看自己的宿舍信息
- 提交报修
- 查看报修进度

### 7. 在线课堂管理员

这里更准确地说，主要是“教师管理自己的课堂”。

教师负责：

- 创建课堂
- 发布公告
- 上传资源
- 删除自己课堂的资源
- 布置作业
- 查看提交情况
- 批改作业

学生负责：

- 查看课堂
- 查看公告
- 查看资源
- 提交作业
- 查看成绩反馈

### 8. BBS 管理员

负责：

- 创建/维护板块
- 审核帖子
- 删除违规帖子
- 删除违规评论

普通用户负责：

- 浏览板块
- 发帖
- 评论
- 删除自己的帖子或评论

## 七、导航可见性规则

左侧导航根据用户权限显示。

### 学生

建议显示：

```text
选课系统
图书馆
校园商店
虚拟银行
宿舍管理
在线课堂
校园BBS
```

学生不显示“学籍管理”。如需查看本人基础档案，后续建议放到“个人中心”或工作台个人信息区，不作为“管理模块”入口。

### 教师

建议显示：

```text
在线课堂
图书馆
校园商店
虚拟银行
校园BBS
```

教师不显示“选课系统”。教师查看授课班级、公告、资源和作业，统一放到“在线课堂”或后续教学相关入口中。如果教师被授权为某模块管理员，则对应模块内显示管理功能。

### 系统管理员

建议只显示：

```text
用户管理
```

系统管理员如需测试其他模块，可以另外建立测试账号，不建议用系统管理员身份直接进入所有业务模块。

### 模块管理员

模块管理员仍然保留自己的基础身份，同时左侧导航显示被授权管理的模块。

例如某教师同时是图书馆管理员：

```text
基础身份：TEACHER
模块权限：BOOK_ADMIN
导航入口：在线课堂、选课系统、图书馆、校园BBS 等
图书馆内显示管理按钮
```

## 八、每个模块页面的统一结构

每个模块页面建议统一分成两层：

```text
普通功能区
管理功能区
```

普通功能区是学生、教师等普通用户能用的功能。

管理功能区只有模块管理员或系统授权人员能看到。

例如校园商店：

```text
普通功能区：
商品列表、购物车、订单、支付

管理功能区：
商品维护、库存调整、订单管理
```

这样比“每个管理员单独一个窗口”更清晰。

## 九、当前设想中需要修正的点

### 1. 不建议开放自由注册

校园系统的账号最好由管理员创建。注册页可以有，但建议做成“申请账号”，而不是直接注册成功。

### 2. 不建议系统管理员拥有所有业务操作

系统管理员负责账号和权限，不负责所有业务细节。业务模块应该由模块管理员负责。

### 3. 不建议把模块管理员做成基础角色

如果新增太多角色，例如 `SHOP_ADMIN`、`BOOK_ADMIN`、`BANK_ADMIN`，后期权限会混乱。

更好的方式是：

```text
基础角色 role + 模块授权 module_admin
```

### 4. 不建议每个模块自己开 JFrame

统一使用 `MainFrame + JPanel`。模块负责人只交自己的 `JPanel`，由主界面负责切换。

### 5. 不建议界面直接操作数据库

界面只调用 client service。数据库只在服务端 DAO 操作。

## 十、建议下一步实现顺序

第一步：完善登录页

- 正式登录
- 登录失败提示
- 首次改密跳转

第二步：完善工作台和左侧导航

- 根据角色显示不同入口
- 点击进入模块
- 返回工作台

第三步：完善用户管理

- 创建账号
- 分配基础角色
- 授权模块管理员
- 冻结/启用账号
- 重置临时密码

第四步：定义模块管理员表

- 新增 `sys_module_admin`
- 新增对应 DTO
- 新增授权接口

当前本地工程已完成：

- `sys_module_admin`
- `ModuleCode`
- `ModuleAdminDTO`
- `ModuleAdminRequest`
- `USER_LIST_MODULE_ADMINS`
- `USER_GRANT_MODULE_ADMIN`
- `USER_REVOKE_MODULE_ADMIN`

第五步：逐个接入业务模块

- 先接学籍
- 再接选课和 LMS
- 再接图书、商店、银行、宿舍、BBS

## 十一、最终目标

最终客户端应该形成下面这种体验：

```text
用户打开客户端
进入登录页
登录成功
如果必须改密，先进入改密页
否则进入工作台
左侧导航展示当前用户能用的模块
点击左侧导航进入对应模块
模块内部根据权限显示普通功能或管理功能
退出登录后回到登录页
```

这个设计既能符合课程项目要求，也比较接近真实校园系统的权限逻辑。
